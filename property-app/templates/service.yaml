apiVersion: v1
kind: Service
metadata:
  name: {{ include "property-app.fullname" . }}
  labels:
    {{- include "property-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "property-app.selectorLabels" . | nindent 4 }}
